import React from 'react'
import ReactDOM from 'react-dom/client'
import OptilensCore from './OptilensCore.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OptilensCore />
  </React.StrictMode>,
)
